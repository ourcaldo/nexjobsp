/**
 * Re-export from canonical location for backward compatibility.
 * Prefer importing directly from '@/lib/cms/interface'.
 */
export type { Job, JobFilters, AdminSettings, SitemapSettings } from '@/lib/cms/interface';